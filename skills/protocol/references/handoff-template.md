# 写 / 改回件模板(①写 · ③改;整块照抄,逐项填;缺项 = 回件不合格)

边写边落。件内只贴判词 + 指针,命令的全量输出与账由 `flow-ev` 落 `.evidence/`,不进件。
**首行逐字写成 `# <轮名> 回件 · …`**(轮名 = 派单「轮次 / 模型」行的字面):`flow-round close` 按它认领本轮回件,认不出 RED。
**〇 表就是申报清单**:第二列 = `路径[:行段]`,每个实改文件 ≥ 1 行;编排方 `flow-round close` 从它派生申报、verify、冻结,你不跑收工命令。表漏一个实改文件 = verify RED 漏申报。
**〇 节不许留占位**;被判停也在原位写实况,续轮在原位补全(〇 是下游整轮唯一的跳读入口)。**〇 + 丙栏 有字节红线**(`FLOW_DOC_BUDGET_HOTPATH`,close 判):细节往甲栏与 `.evidence/` 挪。

```
# <轮名> 回件 · <一句话是什么>

## 〇 · 改动索引表(首节;②③④ 按它跳读;flow-round close 按它派生申报清单)
| 符号 | 文件:行段 | 性质 | 对应 | 例外 |
|---|---|---|---|---|
| `allocate()` | src/engine/alloc.ts:120–188 | 改 | REQ-P4-T2-03 | |
| 新文件 | src/engine/alloc.test.ts:1–96 | 新增 | REQ-P4-T2-03 | |
| `legacyRound()` | src/engine/round.ts:40–71 | 删 | finding B-3 | |
| `helper()` | src/engine/old.test.ts:12–30 | 改 | finding B-5 | 裁决-41 |
第二列路径必填(仓库相对;新增文件写整文件行段);性质只许 新增 / 改 / 删(③改 的反证顶回写 顶回,不进申报);③改「对应」列一律 finding 编号。
例外列:越面、改已有测试件,写编排方给的 `裁决-N`(派生成申报行尾 `# 裁决-N`);没有裁决号的例外 = RED。

## 一 · 结论一行
交付 / 未交付;门末行读数(①写:.evidence/<轮名>-gates.txt 末行);①写 另写 步骤账 已勾 a / 共 b

## 甲栏 · 实测过的
→ .evidence/<轮名>-log.tsv(N 条)
- <名>:<一行判词>        ← 每条 flow-ev 一行,不多不少(close 判 判词行数 = log 行数);命令 / RC / 末行 / 路径都在账里,不抄
## 乙栏 · 未验推断(没有 flow-ev 账佐证的,一条都不许当结论用;核不了就写「核不了」)
## 丙栏 · 自报三处最没把握(①写必填;两条审查轴按它排优先级;引证据写 ev:<名>)
## 丁栏 · 账本追写草稿(每条触到的开放欠账一行:标记 · 还 / 追写 / 不动 · 一行证据 ev:<名>)
## 戊栏 · 给 flow-local.md 的新实录(踩到就写)
```
