# CHANGELOG

版本记事(从 CLAUDE.md 搬来;CLAUDE.md 只留地图与契约)。新版本写在最上面。

1.0.5(止血批,TODO §A 七条;协议零字节变化,agent 面向接口不动,全在 kit 一侧;每条一个提交,两 locale 全绿才下一条):
**A1 `$VAR` 后紧跟非 ASCII 字符一律 `${VAR}`**(23 处:flow-dispatch 9 · flow-round 5 · flow-receipts 3 · flow-close 2 · flow-ev / gates / ledger / trace 各 1):bash 3.2(macOS `/bin/sh`)在 UTF-8 locale 下把变量名后的多字节字符吃进变量名(`」』》】、。「·—` 全炸,`),:;(` 不炸),C locale 看不见 —— smoke 此前只在 C 跑,702/0;同一份在 en_US.UTF-8 是 565/138。smoke 头部改 `LC_ALL=${SMOKE_LOCALE:-${LC_ALL:-en_US.UTF-8}}`(真实会话是 UTF-8,默认按它跑;显式给的照用),§0 加 perl 字节扫描棘轮:`bin/* lib/*.sh tests/*.sh` 命中数只许为 0(tests 也扫:A2 写进 smoke 的一行 `$v1」` 在 UTF-8 下让 `set -u` 半途 abort,回退实验时才炸出来)。
**A2 申报清单的解析收成 lib 一条**:`path  # 裁决-4703,4800 micro` 这一行曾有四套 sed / awk 各剥一遍(flow-close `declp` / `declv` · flow-manifest `declared_paths` / `declared_verdict` · lib 派生并集 · flow-micro 追号就地正则),外加 flow-dispatch ④ 跳③行第五套;任一处漂了就是静默漏(1.0.4 第二个裁决号丢失一族)。lib 新增 `flow_declared_rows <件>` → 每行 `path<TAB>号串<TAB>注释`(剥 sha 前缀 / git 状态位 / rename 箭头;注释行空行不出;累加号原样带出,B2 起行尾词 `micro` / `prod` 由注释列带),`flow_declared_paths` = `cut -f1 | sort -u`,`flow_declared_verdicts` = 有号的行。五处读方改调它;flow-micro 追号改成 读 rows → 改内存中那行 → 整件重写(已有无号行就地补 `# 裁决-N micro`,不再追加重复路径行;kit 头原样保留)。存盘格式**不动**。
**A3 `flow-ev` 多实参直接 exec**:1.0.1 把 argv 重包成 shell 串再靠「裸操作符词放行」让 `-- a '|' b` 当管道,引号 bug 全出在这段重序列化。现在 `$# -gt 1` ⟹ `( cd 仓根 && "$@" )`,零 shell、零重拼,每个实参逐字到达命令;要管道 / 重定向写单串(单串照旧 `bash -n` 先过 + `bash -o pipefail -c`)。log 第五列的单引号重包只做展示(可照抄进 shell);`--show` 守卫留(它是参数解析)。派单 FREEZELINE 那句改成「单串经 bash -c,多实参直接 exec,要管道写单串」。唯一行为变化:`-- echo abc '|' tr a-z A-Z` 现在 echo 收到四个实参、输出含 `|`。
**A4 smoke §27 整条链**:dispatch → open → 回件 → close → ②B(活树-独占)/ ②A(快照,close 印 `⊘快照不冻`)→ flow-micro ×2(同路径,第二份走裁决号累加)→ ④B 派单(断言印「本批跳 ③(裁决-9,10」)→ open(断言核的是重打后的 `.after-②B` 且 FAILED=0)→ close → wrap(①写 基线 + ④B 派生申报,测试锁靠微改行的裁决号放行)→ 状态档零 `⚠glob猜` · 目录零残件。命令之间的缝是 1.0.4 三处 bug 的老家;这条链是批 B 每条的回归床,编排方在链里零次 Edit。回退验收(手工各做一次):回退裁决号累加 ⟹ 链5 + 链6 红;回退 ④ 跳③行 ⟹ 链6 红;回退残件守卫 + `head --` ⟹ 链不红(链里本就没有残件,§26 (5) 三条照红)—— 守卫类修法由阳性用例盖,链只盖缝。
**A5 CI**:`.github/workflows/smoke.yml` 两 OS × 两 locale(ubuntu-latest: C / C.UTF-8;macos-latest: C / en_US.UTF-8),README 徽章。ubuntu 的 `sh` 是 dash,顺手验契约 4 的 POSIX 纪律;红了是真的,不许 `bash tests/smoke.sh` 绕。本地先在 ubuntu:24.04 容器里跑过四格:唯一红是 smoke 自己的 `stat -f %m`(GNU stat 无),改成 `[ ! a -nt b ]`。
**A6 轮级约定文件名收进 lib 一处**:`flow_round_file <目录> <轮名> <baseline|declared|after|face|steps|log|gates|evdir>` 印绝对路径,反向 `flow_round_of <路径>` 取轮名;flow-round / flow-dispatch(含印进派单的步骤账 / 基线名)/ flow-ev / flow-micro(`.after-X ↔ .declared-X` 配对)/ flow-step 全改调它。契约 3 的「四处派生,改一边就要改另一边」是 1.0.4 三处 bug 的同一根。smoke §0 棘轮:bin/lib 里 `"$VAR/.(after|declared|manifest-baseline|face|steps)-` 的代码形命中数 0(注释不算;枚举 glob 不在此形)。
**A7 close 判据上棘轮 + 出处**:`flow-close` 每个 `step_*()` 头一行 `# 抓到过:<批 · 什么>`;smoke `STEP_CAP=17`(量法 `grep -c '^step_[a-z_]*()'`),只许往下改 —— 每个 step 都是一次潜在假红往返(F-58 第五次 RED 才印差集),往后每加一条判据退一条(契约 10 追加)。**无出处判据清单**(归批 / 退役候选,TODO §B7 处置):`step_hollow`(它护的是 verify 的空集假绿,0.8.x BSD awk 那次是人眼发现的)· `step_tree`(读数不判红)· `step_route`(P3-3 那次是输入守卫红,不是漏路由)· `step_trace`(P3-1 六次假红 → 0.7.1 N/A)· `step_lint` · `step_budget`(0.8.0 目录合计线 · 0.8.2 自写近线 WARN 11 次零处置,两条都是假红源)· `step_rulebook`(0.8.1 裸名结构性假红 · 0.8.3 换单位)· `step_verdicts`(dry-run,体例错在 flow-ledger 入口就 FATAL);有出处的:`step_after`(P2-4)· `step_manifest`(quotepath / P3-4 树身份 / 测试锁)· `step_factlint`(P3-4 ②B-F)· `step_face`(P3-4)· `step_gates`(每批);`step_index` / `step_markdone` 是写手,`step_usage` 是账,`step_state` 是刷新,都不是判据。
smoke 747 ok / 0 RED × 四格(macOS C / en_US.UTF-8,ubuntu-dash C / C.UTF-8;+45:A1 棘轮 1 · A2 11 · A3 2 · A4 链 27 · A6 棘轮 + 单测 2 · A7 棘轮 + 出处 2);`GEN_CAP` 2500 不动(实测 2249)。

1.0.4(按 P4-T 实测修十二处;agent 面向接口不动 —— 十二处全在 kit 一侧:两条判据松口(3 / 2)、一条判据缩范围(9)、其余是守卫与读数):
**`flow-receipts` 轴 3 跨件 + `RECEIPTS N/A`**(1 / 6,本批最贵的发现):【裁决 558】把 P4-T3 的接收位写在 p3 §P3-T6(owner 标 P4-T3),派 P4-T3 时只扫 p4 ⟹ 零字,五刀收工没人看见 → 生产投影零刷新通路(`#108`)。轴 2 的定义本就是「别处落给它的账」,只是「别处」止于同一份文件 —— 扩一条轴 3:`FLOW_SPEC_DIRS` 下**别的** md 里同时点名任务号且带标记词的行(`file:line`),派单接收位段里就有它;只列带标记词的行,不列所有提及(总表会把每个任务号都列一遍)。不做 `--lint-all`:它要先定义「X 节里的对应指针」是什么(源任务号?裁决号?),那是一条新的 plan 写作约定。标记词 `接收位|强制条款|开口项` 从脚本搬进 config `FLOW_RECEIPT_MARK_RE`(契约 2)。section 体例下节在而三轴全零 ⟹ `RECEIPTS N/A` RC=3(印节行数,§J0;同 `TRACE N/A`),不再与「节不存在」同判 FATAL 并被原样印进派单;table 体例三轴全零 = 任务号不在表里,仍 FATAL。
**〇表性质按前缀认 + WARN 点名**(2):`flow_handoff_paths` 只认 性质 ∈ {新增,改,删} 精确串,①写 写成「编排方预改(基线内)」⟹ 静默不进申报 ⟹ close 只见 `RED 漏申报`,病因看不见。改:性质按前缀 `^(新增|改|删)` 认(`改(编排方预改)` 能过);不认的性质而第二列像路径的行由新库函数 `flow_handoff_skipped` 列出,`flow-round close` 印 `WARN 〇表 L<n> 性质「…」不在 新增 / 改 / 删,未进申报: <path>`。**不**放宽到「有路径就进申报」:申报 ⊆ 面 是越面判据,「核过 / 顶回」行进表会把没改的面外文件判成越面 RED(假红换假绿)。WARN 由 close 单独一趟印,不进 lib(`flow_handoff_paths` 在 close 里被调两次,放 lib 会印两遍)。
**甲栏判词改按名集判 + 印差集**(3,F-58 第五次 RED):close 只印「24 ≠ 25」,差在哪一行要人手工 diff log 名与甲栏名。改:判词行也认 `- 名(…):` 形;提名集合与 log 名集比,**等数不等集同样红**(行数只是集合相等的代理量:判了没跑的、漏了跑过的);红时印 `log 有 / 甲栏无` · `甲栏有 / log 无` · `甲栏重复名` 三行。
**节选切点对齐段落边界 + 默认 16000 + `--excerpt-bytes`**(4):`tail -c` 按字节切、`sed 1d` 只丢半行,P4-T 的落位段是一整段 blockquote,被中间截掉,读侧 / 写侧两条落位成了「按需 sed」(漏读入口)。改:按字节定的切点往前退到最近空行之后(段落 / blockquote 整段保留),**回退有界**(≤ 节尾预算 `rem`;窗内无空行就切在字节点并点名「切在段中」—— 无界回退会退到节头整节印出,smoke 那种 400 行连排的节封顶失效);节尾标题印「切点已前移 N 行」。不认「blockquote = 落位」这条项目约定,按 markdown 段落切是通用的。默认 12000 → 16000(只有 ①写 读正文;smoke 的 GEN_CAP 量法不带 `--plan`,棘轮不动)。`--excerpt-bytes N` 单次覆盖;**不做**环境变量覆盖 —— lib「默认值 → source config」的次序是有意的,env 泄漏进来会静默改读数。
**`--face-from` + 残件守卫**(5):触面每轮重打一遍(15–18 条路径手敲四次),zsh 不分词又塌出 `-dispatch.md` 0 字节残件,之后每次 dispatch 的 `head` 都报 usage。改:`flow-dispatch --face-from <.face-<轮名>.txt>` 读上一轮的面并与命令行触面取并(扩面 = 指原件 + 多给几条;与 `--lane-merge` 互斥);`flow_find_handoff` 的 `head -1` 加 `--`;新库函数 `flow_check_flow_dir` 在 `flow-dispatch --dir` 与 `flow-round` 入口对以 `-` 开头的 md 件 FATAL 点名。**0 字节件不能判死**:`flow-dispatch … > $DIR/02a-dispatch.md` 里 shell 先把输出件截成 0 字节再起进程,扫目录必撞见自己的输出件 —— 静默跳过(首行空串本就认不出),smoke 有它的阴性对照。
**裁决号累加**(7):`flow-micro --apply` 对申报清单里已带裁决号的路径印「已有,不追加」,第二个号丢了,派单里数出来的「六行」实为五行。改:就地追加成 `# 裁决-4703,4800 micro`(已含本号不重复);三处消费者(`flow-manifest` 测试锁 / `flow-close` 越面与交付态 / lib 〇表)都只取首个号,向后兼容,派生并集原样带号。
**`flow-trace` 只认标题行**(8,②A 发现 `console.log` 里的 `[REQ-…]` 被计成覆盖):命中 = 含 `[REQ-ID]` 且本行匹配 `FLOW_TEST_TITLE_RE`(kit 默认 `it( / test( / describe(`,含 `.each` 等链式),**或上一行匹配且以 `(` 结尾**(prettier 把长标题折到下一行:`it(\n  "[REQ-X] …",`,单行 grep 会把真命中判丢);其它命中单列「非标题 M 处不计」,不静默丢。标题词进 config(`FLOW_TEST_GLOBS` 已为 go / py / java 留了口,标题行同理);带反斜杠的模式走 ENVIRON 不走 `awk -v`(-v 再吃一层转义);模式为空 FATAL(零模式不许判零覆盖)。
**快照轮 close 不核活树**(9):快照轮结构上写不到活树,而并行的活树轮(②B 变异)正在变异中间态,旧法只能等 ②B 收完再 close ②A。改:`flow-round close` 从本轮派单的「树权限」行认(与 `--resume` 的 RTREE 同一条 sed,`find_dispatch` 已有),快照 ⟹ `flow-close --between --snapshot`:verify · 非空转 · 假话门 · 越面 四步印 info 不跑(树读数 · 路由复跑 · REQ 对账 · 预算照跑),close **不冻结**,状态档冻结件格印 `⊘快照不冻`。「快照轮误写活树」的守卫不丢:下一轮 `flow-round open` 核最新 `.after` 对活树,写了就 FAILED。派单认不出(glob 猜)⟹ 照旧全跑 + WARN。用结构性规则不用「检测同批有没有活树轮未收」:跨轮推状态是新状态,结构性规则零状态。
**④ 派单的「③改 回件」行自动印跳 ③**(10):没有 `# ③改 回件`、但目录里的 `.declared-*.txt` 有 flow-micro 追加的 ` micro` 行 ⟹ 印「本批跳 ③(微改通道已落笔,裁决-N,M…)」(升序去重),不留待填;取 `.declared` 的 micro 行不取 `.after-*.pre-N` 件名 —— 前者带号与路径,后者只有号。
**行内删字按协议量**(11):`references/dispatch-block.md` 早把写文档 / 注释轮的判据定为「new ⊂ old ⟹ 新增字符 0」,`flow-micro` 却按 numstat 行数量 —— 量法与协议不同源,只删不写的 6 行读成「非测试新增 6」。改:kind=注释 的文件把 patch 落进临时副本(POSIX `patch -p1`,不用 `git apply` —— 仓外路径要 `--unsafe-paths`,语义不稳),整文件 new 是 old 的字节子序列 ⟹ 该文件新增计 0(读数行括号里留 numstat 原值),不成立照 numstat。范围只在 注释 kind(生产恒 RED、测试不计),「删字伪装改语义」的口子只在文档 / 注释行。
**F-39 `flow-ledger apply` 证据栏不截断**(12):`--write` 把证据截在 100 字节、dry-run 预览截在 60,`ev:<名>` / `file:line` 指针切成半截写进账本就断链(§J 硬规矩①)。两处都改印全文。F-59 已在 1.0.2 落地(`dispatch-block.md` 的 `--no-index` 那句),本批不再动。
smoke 702 ok / 0 RED(+84:§26 十二处各有阳性 + 阴性对照;§16 甲栏两条读数随判词改写);`GEN_CAP` 2500 不动(实测 2249)。

1.0.2(按 P4-T 实测修五处工具错 + 换派单块一句;agent 面向接口不动,新守卫全在 kit 一侧):
**`flow-ev` 三修,同一个根 —— 名在跑之前就被烧掉、RC 在管道里被换掉**(F-60 / F-62 / F-63):① 改用 `bash -o pipefail -c` 跑 —— 旧 `sh -c` 下 `… | grep … | cut …; echo rc=$?` 印的恒是 cut 的 0,④A 一整组 `rc-*` 读数因此无效(与 F-53 同族:RC 隔管道取);代价是判据后接 `head` 截大流得 SIGPIPE 141,那是响的错不是静默的 0,§J① 本就禁这么写。② 跑前 `bash -n` 语法校验(与运行同一解析器),错了 FATAL 且**不建 OUT、不记账** —— 旧版单实参串里嵌 `\"` ⟹ `sh -c` 语法错 RC=2,而证据件已建、名已烧(`scan-positives` 作废只能起 `scan-positives2`)。③ 多实参里出现 `--show` 当场 FATAL —— 旧版把它重包进命令串跑成 `sh: --show: No such file` RC 127,名同样烧掉。三条守卫都排在建 OUT 之前,同名可重来。
**`flow-usage` 漏掉收口会话**(F-64):`--ship --dir` 末尾重跑轮数账,窗仍止于主会话末 —— 父会话是从命中的 sub-agent transcript 反推的,而收口常是另起的一个**零 sub-agent** 会话(p4t 实测:主会话 14 个 sub-agent 12:09 收,收口会话 12:10–12:33 零 sub-agent、提到 `.flow/p4t` 82 次、跑了真 `--ship`,账上没它)。补扫 parent 同项目目录下的顶层会话:mtime ≥ 首个 sub-agent 起点(挡住 177 MB 目录的大头,整跑 0.5 s),且某一条工具输入**同时**含 `flow-close` 与本流程目录(`flow-close --wrap <目录>` / `--ship … --dir <目录>`;只提到目录不算 —— 下一批读本批回件也会提到它,smoke 有阴性对照)。多个父会话的窗取并(最早起 → 最晚收),`.usage.md` 固定开销表的「收口」段从此有读数(p4t:2 会话 / 收口 1m → 3 会话 / 收口 14m)。
**`flow-ledger touches <标记> <a,b>`**(F-61):机器头 `touches:` 并集追加,去重保序,仓库相对守卫与 `add` 同一条,落盘后照旧自动 lint —— 三态表「追写」常带触面变宽(P4-T `#102` `#97` 加 `apps/web/src/screens/reports`),此前只能 sed 直改机器头,「落笔一律走 flow-ledger」在这一格做不到。不做泛化的 `set <字段>`:title 要裸文件名守卫,owner / due 没观测到需求。
**派单块 ④B 出 patch 那句换掉**(F-59,`references/dispatch-block.md`,生成段零字节变化):旧句「改 → `git diff` 出件 → 还原」对本批已是 `M` 的文件是错法 —— `git diff` 印的是 HEAD 起的整段,编排方在当前树 `git apply` 撞已落的 hunk。新句 = `cp` 备份 → 改 → `git diff --no-index <备份> <文件>` → 头两行备份路径换成 `a/<仓库相对路径>` → 从备份还原 + `shasum -c`;临时仓验过改头后 `git apply --check` RC=0。不选 `GIT_INDEX_FILE=<tmp>` 那条路:漏写一次环境变量就写真索引且 patch 静默退回整段,cp 法漏改头是 apply 当场炸。
**`flow-micro --apply` 的「已有(带裁决号)」印出具体号**(非 bug;上一会话状态档把 4490 误记成「已追加」,印出 `已有(带 裁决-4400,不追加 裁决-4490)` 就不会再记错)。
smoke 618 ok / 0 RED(+15:flow-ev 三组 · flow-usage 收口会话 + 阴性对照 · touches 五条);`GEN_CAP` 2500 不动(实测 2249)。

1.0.1(按 P2-4 实测修两处工具错;agent 面向接口不动,两处都在 kit 一侧):
**`flow-ev` 引号拍扁** —— 旧写法 `CMD="$*"` 把 `--` 之后的实参用空格拼成一串再交 `sh -c` 二次分词,调用方的引号边界全丢:`-- sh -c 'shasum -c X; echo …'` 拼成 `sh -c shasum -c X; echo …`,内层只跑到 `shasum`(读空 stdin 印 SHA-1),`-c X; echo …` 落到外层;`-- grep -F '"—"' src` 的双引号被外层 sh 剥掉,模式成裸 `—`,数出 526 行假读数(②B 回件 :12 / :27)。修法向后兼容:**单实参**整串照旧当 shell 串(`-- 'a | b'` 写法不变);**多实参**逐个重包成 POSIX 单引号串(`'` → `'\''`;只含 `[A-Za-z0-9_.,:/=@%+-]` 的实参不包,log 第五列仍可照抄),只有裸操作符词 `| || && ; & > >> < 2> 2>> 2>&1 ( )` 原样过,`-- a '|' b` 的管道照通。唯一行为变化:`-- ls '*.ts'` 以前会被内层展开,以后按调用方写的引号当字面 —— 这是修正不是回归。
**`flow-close --wrap` 步 1 对收口编辑恒 RED** —— 步 1 拿上一轮收工冻结的 `.after-<轮名>-hashes.txt` 核活树,而收口本身有编排方按裁决必须动树的编辑(裁决-2291 删 `check.sh` 的 `KNOWN_OVER` 两行),它发生在 wrap 之前 ⟹ 该件哈希必 FAILED;脚本原话「FAILED 若全在编排方并行编辑面则不算红 —— 由编排方自判,本脚本按红报」= **知道却照红**,每批都要手工 `flow-freeze` 重打(`.pre-2291` 就是那次的备份)。修法接上申报清单里现成的例外通道(测试锁 / 越面都走它):FAILED 的路径在申报清单里且申报行尾带 `# 裁决-N` ⟹ 印 `ok   编排方收口编辑(裁决-N): <path>: FAILED` 不计红;无号照红并给改法。WRAP OK 之后 kit 把这些路径在 `.after` 里**就地重打**(旧件 `<after>.pre-wrap`;树上已没有的路径删行,与 `flow_freeze_to` 只列存在文件同口径;备份件名不在 `.after-*-hashes.txt` 的 glob 里,`flow-round open` 取最新不会取到它),红了不动冻结件。不加 `--refreeze` 开关 —— 例外必须带号能追到裁决。`declv`(裁决号表)从越面那步提到顶层,越面与交付态共用一份解析。
两处各加回归探针(smoke §24 末尾 / §25):`-- grep -F '"x"'` 只命中带引号的行、`-- sh -c 'a; b'` 两句都跑、裸 `'|'` 当管道、引住的 `'*.txt'` 按字面;带 `# 裁决-N` 的改件 + 删件 ⟹ 步 1 绿、WRAP OK、`.after` 重打后对活树全 OK、`.pre-wrap` 留旧内容;无号 ⟹ `RED  <path>: FAILED` 点名、WRAP RED、冻结件一字不动。smoke 603 ok / 0 RED。

1.0.0(瘦身:agent 面向接口定型;三批 33 会话实测 sub-agent 请求 28–36% / 携带 30–41% 是流程开销 —— 收工仪式、逐段打字的甲栏、读 kit 源码,全砍):
**A1 收工零命令** —— agent 收工只写回件;申报清单由 `flow-round close` 从回件〇节索引表派生(第二列 = `路径[:行段]`,新增「例外」列写 `裁决-N` ⟹ 申报行尾;〇表路径 ∪ 目录里所有先前轮的派生清单 —— git 轴比的是 HEAD 不是基线,取并集而不取「上一轮」是因为 ②A ‖ ②B 谁后收工不定、flow-micro 又会追写 ①写 那份),落 `.declared-<轮名>.txt`(kit 头;目录里有 agent 手写的同名件 ⟹ diff、WARN、以派生为准);between 全绿后 kit 打 `.after-<轮名>-hashes.txt`(红了不冻);「交付态哈希」从 close 挪到 open(核目录里最新的 `.after`,抓轮间动树;一批第一轮无则 info)。`flow-close --between` 不再收 after-hashes(给了 FATAL),加 `--this <本轮路径清单>` 让越面只判本轮〇表路径、不判并集。`flow-freeze` 主体入库 `flow_freeze_to`,命令留壳。lib 新增 `flow_find_handoff` / `flow_handoff_paths` / `flow_derive_declared`。
**A2 甲栏机器生成** —— 新命令 `flow-ev <流程目录> <轮名> <名> [--show N|all] -- <命令…>`:在仓根 `sh -c` 跑,全量落 `.evidence/<轮名>-<名>.txt`,RC 同层捕获,往 `.evidence/<轮名>-log.tsv` 追加一行(名 · RC · 末行 ≤200 B · 时间 · 命令),屏上印末 N 行 + RC;同名 FATAL。回件甲栏只留每条一行判词 `- <名>:<判词>`,丙栏每条引 `ev:<名>`。`flow-round close` 三判:审轮 log 非空 · 丙栏 ev 在 log 里有行 · 甲栏判词行数 = log 行数(「贴了 N 行 / 判了 N 行」的机器版;复审模板「必答四组数」退第 2、4 组)。独占轮门整跑写成 `flow-ev … gates -- flow-gates --reset`(落的正是约定名 `<轮名>-gates.txt`)。
**A3 步骤账只归 ①写** —— `flow-dispatch --dir` 只给 ①写 建 `.steps`,②③④ 不建,close 对它们改 info;`--resume` 无步骤账不再 FATAL:扫回件 `## ` 节头与 log 行数印「已落 / 缺」作接手点,回件一个字没落就从头。
**A5 判据自解释** —— `flow-fact-lint` 有 RED 时印族名图例;`flow-manifest verify` 的 RED 行注明轴口径(git 轴 = 自 HEAD 的未提交改动;ignored 轴 = 与基线比);回件模板整块印进派单生成段的 `<!-- flow:tpl-begin/end -->`(smoke 的 GEN_CAP 量法排除它;「不许手打插件缓存路径」那条禁令随之作废);config 的门名单印一行。
**B2 / B3 派单** —— 收工段(flow-freeze / .declared / verify / fact-lint / doc-budget)与 ②③④ 步骤账行整段退出,散文里的墙钟数字回 SKILL,`GEN_CAP` 3827 → 2500(实测 2249);必读骨架由 kit 印:上一轮回件按首行认领(②←①写;③←②A ②B;④←③改 + ②A ②B 丙栏,缺的留待填不静默)· flow-local 路径 · 路由到的每条欠账的账本行区间 `debts.md L519–535(#47)`。手写段只剩裁决与必查。
**C1 微改通道前移到 ②** —— ② 收工后编排方 `flow-micro <②A+②B patch…> --face .face-①写.txt --freeze <最新 .after> --apply --verdict <N>`;两轴 patch 先拼成**一个流**再 --check / --numstat / apply(`git apply --check a b` 是逐文件对原树各查一遍,两份改同一行也 RC=0,而 `git apply a b` 会落下第一份、在第二份上失败 —— 半截落笔,实测;0.8.0 起就有这个潜伏病)。`--apply` 后 kit 自动三件:冻结件备份成 `<冻结件>.pre-<裁决号>` 再重打 · 向配对的申报清单(`.after-X-hashes.txt` ↔ `.declared-X.txt`,或 `--declared`)追加 patch 触到的路径 `# 裁决-N micro` · 印每份 patch 的复现头(`diff --git` 之前以 `#` 起的行:模板让审方写 `# repro: <命令> → 期望 <读数>`)。两轴丙栏每条都带 patch 且 MICRO OK ⟹ 跳 ③ 直接 ④。
**C2 上限只数非测试行** —— numstat 按 `FLOW_TEST_GLOBS` 分两桶,门槛只看非测试桶,末行印「非测试 N · 测试 M」;`FLOW_MICRO_FIX_LINES` 语义随之改。
**D1 文档** —— SKILL.md 29 → 17 KB,只留规矩 + `→ why §x`;实测与病根整段搬进 `references/why.md`;本文件的 0.7.1–0.9.0 版本记事搬进 CHANGELOG.md,只留地图与十条契约。
**D3 flow-usage** —— 轮次行加 sub-agent 开销三列(仪式 / 读 kit / 打回件,按请求计、一请求一类:打回件 > 仪式 > 读 kit;分类只认命令头与路径形状,不进 config),合计行带占比,> 15% WARN。这是下一批的验收读数(p4n 72 / 13 / 42 → ≤10 / 0 / ≤25)。
明确不做的:①写 test-first 指令 · 「不许改测试」类散文 · 派单里「cached ⟹ 另跑」· 项目侧四件(量开局 52k 构成 / touches 收窄 / 换模型对照 / Stryker 门)。

0.7.1(工具修):`flow-trace` 学 table 体例(节 = **首格恰等于任务号**的表行,不用 `flow-receipts` 轴1 的全行 `index()`)+ 整份 plan 零 REQ-ID 出 `RC=3 / TRACE N/A`,`flow-close` 的两处对账按 info 处理不判红;`flow-round` 的派单 / 回件两列改**按件里的内容认领**。

0.8.0(微改通道换量法):新 `flow-micro <patch>... --face <写权限面清单> [--freeze] [--apply]` —— 行数由 `git apply --numstat` 的**新增行**求和给,性质由路径(`FLOW_TEST_GLOBS` / `*.md`)与 patch 改动行核,面内按 `flow-dispatch --dir` 落的 `.face-<轮名>.txt` 判;`flow-doc-budget` 的目录合计线按轮数归一(轮数 > 6 时线 = DIR × 轮数 / 6)。

0.8.1(按 P3-2 实测修四处工具错):`flow-merge-lane` 主树缺失的两种语义分开 —— 基线里**也**没有 = 支新增件(`NEW`,直取支树版,apply 补建父目录),基线里**有** = 主树删件 vs 支树改件,那才 FATAL(三方逻辑本就接得住新增,卡住它的只是两个写成对称的 `[ -f ]` 守卫;`NEW` 块排在空比对守卫**之前** —— 空新增件的 base 与 theirs 都是空文件的哈希,次序反了就成只在空件上现形的窄误诊);`flow-rulebook` 归档模板的文件名进反引号(裸名撞仓侧「纯文本指针」棘轮,而协议 §5 每批 ≥1 条退役 ⟹ 退役越勤门越红,是**结构性递增的假红源**);`flow-close --ship` 在 clear-map-debt 与 push 之间按 pathspec 补一笔队列提交(clear-map-debt 只写工作树,中间零提交 ⟹ 队列清空**按定义**进不了本批自己的 PR;不 amend 收口 commit,is-ancestor 判据照旧);`flow-pr-merge` 的 `gh pr merge` 输出不再丢进 `/dev/null`(§J 硬规矩①:只报 RC 时「重跑就好」与「真被分支保护挡了」在读数上同型,而这条失败是间歇性的)。另加一条守卫(出处不是 P3-2,是 StockSteer P4-T4-2G 派单实踩):`flow-dispatch` 的**单个触面实参含空白当场 FATAL** —— zsh 不对 `$VAR` 做词分割,把触面清单塞进一个变量喂进来,N 条触面塌成 1 条、欠账必读跟着塌,而派单块照样生成、RC=0(与绝对路径那条同族:让路径轴静默失效的输入,要在入口红,不能靠人记得)。

0.8.2(按 ShipLedger P3-3 实测修八处工具错 + 换掉一条没人理的 WARN):**续轮不是新轮** —— `flow-dispatch --resume` 的轮名改从原派单的「轮次 / 模型」行取(`--round` 可省;给了必须一致;**带 `-续` 当场 FATAL**;原派单本身是续轮派单也 FATAL,续轮不接续轮),`flow-round open` 的轮名带 `-续` 同样判死(续轮沿用原轮的基线 / 步骤账 / 申报清单 / 交付态哈希,开新基线就把一轮劈成两半)。病根实测:编排方把上次生成的续轮名又喂回 `--round`,拼出 `①写-续-续`,回件与派单两格退回 ⚠glob猜。· `flow-round` 的**门读数按约定件名精确取**,不再 glob `<轮名>-*gates*.txt`:`①写-` 前缀吃到了 `①写-续-gates.txt`,于是 ①写 那行印着一次它根本没跑过的 PASS(该轮门整跑 0 次);末行也不再 `cut -c1-60` 切半截路径(违 §J 硬规矩①),改成整段去掉 `out=`。· **词分割守卫上升为库函数** `flow_check_ws_args`,`flow-dispatch` 与 `flow-route-debts` 共用:后者踩了两次(实改集 15 条塌成 3 条,复审轴拿它 diff 派单必读,漏路由整条判据静默失效)。`flow-manifest` 的扫描区**故意不用**这条 —— 那批实参在每个消费者那里都是不带引号展开的,塌了再分开是等价的。· `flow-close --wrap` **先核交付态再动树**:两个写手(`flow-render-index --write` / `flow-trace --mark-done`)原本排在交付态哈希 / verify / 非空转**之前**,而 plan 与索引文档都在申报清单里 ⟹ 核的是 kit 刚改过的树;本批没炸只因为 plan 零 REQ 行(TRACE N/A,零翻转)。两个写手另外都挪到 `red=0` 之后。· **post-commit shim 烙上本树物理路径**:快照是 rsync 副本(连 `.git` 一起),副本里有它自己的 config,`pwd == FLOW_REPO_DIR` 那条守卫在副本里照样成立 —— 在快照里 commit 一笔基线(对 HEAD 出 patch 的正解)就会把队列写进快照、改掉刚冻结的树;`flow-init` 重跑会自动重写自己生成的旧 shim。· `flow-usage` 两修:**门整跑次数改数 `cache.tsv` 窗内 `reset` 行**(证据件按 `<轮名>-gates.txt` 命名,同轮跑两次覆盖同名件 ⟹ 旧口径 4,真值 5,而数漏的那次恰好就是破「轮内 ≤1」的那次);**角色表认出 `③改二` / `③改三` / `定点变异`**(前者退化成 `③改`,后者在 ROLE_RE 里没 token、退到全文扫就撞上派单里提到的上一轮 —— 十轮跑完账上只有八行、③改 那行是 4 个会话,一条复审轴被记成改轮时间,前后批不可比;修后拆成 15m / 20m / 12m / 25m)。· 回件三判归 `flow-round close`(只判**本轮自己那份**):首行认不出 **RED**(从前退回 glob 猜,而猜错的件名进状态档的每一次刷新)· **〇节留占位 RED**(被判停的 agent 留 `(待收工填)`、续轮把结论补在末尾另起一节 ⟹ 下一轮没索引可跳,实测 〇节 52 字节 ⟹ 下游整读 33 KB)· **热路径节(〇 + 丙栏)> `FLOW_DOC_BUDGET_HOTPATH`(新,默认 8000)RED**。同时 `flow-doc-budget` 的「自写近线 90%」从 WARN 降成 **info** —— 那条 WARN 在一个批次里对同一份件喊了 11 次、一次都没被处置:喊的时刻不对(件是上一轮的产出,作者早已下线),对象也不对(实测九份回件全文 16–37 KB,按申报件归一 609–1498 B/件,**全文长度是交付面的函数,不是写作浪费的函数** —— 与 0.8.0 修掉的目录合计线同一种病);真正被下游每一轮重付的只有 〇+丙,占全文 9%–35%。

0.8.3(退役量得到:子句退役 + 棘轮换单位):`flow-rulebook retire` 加 **`N.k` 子句模式**。病根是**工具与需求不对齐**:实录一族一行、族内用 ` · ` 串子句(ShipLedger 两行装了全书 86% —— 5772 / 6685 字节),而 `retire` 只按整行走 ⟹ P3-3 的首选退役对象「zsh `$VAR` 不分词」(已由 0.8.1/0.8.2 的空白守卫接管)是 `flow-local.md:12` 里的一个子句,退不动,那批**零退役**收的口。切分按 ` · ` 且**只切括号深度 0**(`()（）「」『』《》[]【】`,反引号内不切):朴素切法把第 8 行的号段枚举 `(P0-1 占 1–50 · P1-1 …)` 劈成八段、把两条带括号的子句各劈成两半;落盘前断言「子句拼回去逐字等于原行」,拼不回就 FATAL(切分器错了不许写树)。行首列表符与**标题**(到首个深度 0 的冒号为止,半角全角都认、`://` 不算)是 k=0 不可退 —— 退第 1 个子句时标题留在原地,`retire N` 才是整行走;`show <行号>` 印子句号与每条的字节数,子句号是唯一入口(不传字面串:这工具的病根就是 shell 里传中文串污染过归档件)。· **棘轮同批从行换成字节**(`.rulebook-bytes`):子句退役不动行数 ⟹ 旧单位上「退了一条」与「没退」同读数 13 行,退役这个动作机器看不见 —— 与 0.8.1 修掉的「归档件裸名 = 结构性假红」同族,方向相反。行数只剩 `FLOW_DOC_BUDGET_RULEBOOK` 那道**族数**上限(仍会因新增一族而红,不是死门)。换单位那一步**先按旧单位判一次、不红才打新基线并删旧件**(§J:换单位不许洗掉一次变长),新旧两个方向的读数都印;`flow-freshness` 在只剩旧 `.rulebook-lines` 的工作区照旧按行判(不发假警),下次 `flow-close --wrap` 自动换。

0.9.0(按 ShipLedger P3-4 实测修七处 + 按 Böckeler《TDD inside the agent loop》与 2026 年 harness / reward-hacking 一手件换三处量法;原则见契约 9):**假话门进 close**(`flow-close` 的 between / wrap / 默认收工都跑 `flow-fact-lint verify` —— 此前它只在 `flow-round open` 与 agent 自陈里跑,②B-F 的微改 patch 带进一行 RED NEW QUANT 潜到下一轮 open 才炸;修的不是 flow-micro,是「判据没有一个由编排方跑的位置」)· **基线携带树身份**(`flow-manifest baseline` 头记 `root: <git rev-parse --show-toplevel>`,verify 的树 root 不同 ⟹ FATAL:`FLOW_SPEC_UNTRACKED=0` 时 git 轴根本不读基线,主树与 worktree / rsync 快照同 HEAD 就同基线,P3-4 四条 A 轴在副本里跑 verify 报 git 12 与主树同型;配套:快照轮派单不再印 verify,收工行换成对上一轮 `.after` 的 `shasum -c`,续轮同)· **写权限面同源 + 越面判据**(派单「许写」由 `flow-dispatch` 从触面参数印,与 `.face` 同源,禁写留待填;`flow-close --between --face` 判 申报 ⊆ 面,`flow-round close` 按约定名传 —— `.face` 落了三个版本,唯一消费者是 flow-micro,P3-4 三轮申报里的 `apps/web/test/` / `packages/core/src/index.ts` / `i18n/en.json` 面里都没有,没人量;越面 RED)· **测试锁**(基线逐件记 `FLOW_TEST_GLOBS` 命中件哈希,verify 时已有测试改 / 删要申报行尾 `# 裁决-N`,本轮动过的测试件含 `FLOW_TEST_SKIP_RE`(.only / .skip / xit…)同样 RED;`# test-lock N` 头行把「零测试件」与「旧基线」分开(§J0)。病根:测试件在写轮写权限面内,削断言零个工具看;SpecBench / BAITBENCH 实测靠 prompt 说「别改测试」>50% 照改,锁住才归零)· **缓存出处**(`flow-round` 状态档门末行只删 `out=<路径>` 一个词,`(cached…)` 留着 —— 0.8.2 的 `s/ out=.*//` 把它一并删了;`flow-close --wrap` 门读数行带 `cached@<那次整跑的时间>` —— 命中缓存时 `$o` 是缓存条目的证据目录,根文档那句门读数就是上一次整跑的;不加「cached ⟹ 另跑」的协议句,要独立复算是 config `FLOW_GATE_REBUILD` 的事)· **追写认领**(`flow-clear-map-debt` 认 `>` + 任意空白 + `**今天`:`flow-ledger append` 落的是 `>   **`(kit 自己文档化的续行体例),旧判法只认单空格,P3-4 收口 5 条追写全留给人;smoke 阳性改经真写手 `flow-ledger append` 造 —— 从前手工 printf 一个读方格式的行,写读两方从没对过)· **裸文件名 title**(`flow-ledger add --title` 反引号外的裸文件名 FATAL,`FLOW_BARE_PATH_RE` kit 默认、项目按棘轮式覆盖:title 原样进机器头再被 `flow-render-index` 印进根文档,仓侧纯文本指针棘轮到 wrap 门整跑才红,跑了两遍)· **①写设计步在前**(步骤账第 0 步「全部 open REQ 的数据模型 / 接口 / 边界一次列出」;Böckeler 评测里非 TDD 组赢在先出整体设计,n 小、成本一行)· **变异门走 config**(`flow-changed [--no-tests]` 打印实改集给门命令,`examples/` 给 Stryker 一行样例;有 `mutate` 门的仓 ②B / ④B 只判幸存变异体,六守卫不适用)· **审轴校准用存档 gold**(SKILL 换模型那条:`flow-review-diff <gold> <新审>`,改 review-template 前也对一次)。**派单生成段净减**:三种轮各 −90 / −408 / −90 字节(散文里的实测数字退回 SKILL,agent 每轮不必重读为什么),smoke 里 `GEN_CAP` 棘轮只降。明确不做的:①写 test-first / red-green 指令(3–8× token,结果不变)· 「不许改测试 / 不许作弊」类散文(锁,不劝)· 契约文件(flow-trace 的 REQ↔测试就是契约)· 旧树红检(抓不到 `expect(f(x)).toBe(f(x))`,mutate 门抓得到)· 派单里「cached ⟹ 另跑」。
