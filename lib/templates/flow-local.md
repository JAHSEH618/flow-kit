# flow-local —— 本工作区的本地规矩与实录

通用协议在 plugin 的 `/flow-kit:protocol`(按需加载)。这里只放**本项目才有**的东西;规则书棘轮只量本文件:行数只许降,退役件逐字归档到同目录 `flow-local-archive.md`,原位留一行指针。

## 门与单点资源

- 门:见 `flow.config.sh` 的 `flow_gates`(读数命令与 PASS 判据都在那里)
- 单点资源(只许一个持有者):<开发库 / 端口 / 外部沙箱 …>

## 本地规矩(每条一行 + 一句为什么)

- <例:规格目录被 gitignore,全仓 grep 要显式带它 —— 否则「零命中」是假的>

## 裁决号进度

- 下一批从 <N> 起

## 实录(复审戊栏的归宿;踩到就写,一条一行)

- <日期> <一句话:谁 / 命令 / 读成了什么 / 真值是什么>
